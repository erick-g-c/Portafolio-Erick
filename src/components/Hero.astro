<section class="hero" id="hero">
  <div class="hero-content">
    
    <h1>Hola, soy <span>Erick García</span></h1>
    
    <h3>Frontend Developer Jr</h3>

    <p>
      Construyo interfaces modernas, rápidas y enfocadas en la experiencia de usuario.
    </p>

    <div class="social-media">
      <a href="https://www.linkedin.com/in/erick-gabriel-garc%C3%ADa-cortes-11b742354" target="_blank">
        <i class='bi bi-linkedin'></i>
      </a>

      <a href="https://github.com/erick-g-c" target="_blank">
        <i class='bi bi-github'></i>
      </a>
    </div>

    <div class="buttons">
      <a href="/cv.pdf" class="btn" download>Descargar CV</a>

      <a href="https://wa.me/525572085996" target="_blank" class="btn-whatsapp">
        <i class='bi bi-whatsapp'></i>
      </a>
    </div>

  </div>
</section>

<style>
.home {
  min-height: 90vh;
  display: flex;
  align-items: center;
  justify-content: center;
  text-align: center;
  padding: 4rem 2rem;
}

.hero-content {
  max-width: 700px;
}

h1 {
  font-size: 3rem;
  margin-bottom: 1rem;
}

h1 span {
  color: var(--primary);
}

h3 {
  color: var(--text-secondary);
  font-weight: 400;
  margin-bottom: 1rem;
}

p {
  color: var(--text-secondary);
  margin-bottom: 2rem;
  line-height: 1.6;
}

/* SOCIAL ICONS */
.social-media {
  display: flex;
  justify-content: center;
  gap: 1rem;
  margin-bottom: 2rem;
}

.social-media a {
  width: 45px;
  height: 45px;
  border: 1px solid var(--border);
  display: flex;
  align-items: center;
  justify-content: center;
  border-radius: 50%;
  transition: 0.3s;
  color: var(--text);
}

/* HOVER SOCIAL */
.social-media a:hover {
  background: var(--primary);
  color: white;
  transform: translateY(-3px);
}

/* BUTTONS */
.buttons {
  display: flex;
  justify-content: center;
  gap: 1rem;
  flex-wrap: wrap;
}

.btn,
.btn-whatsapp {
  padding: 0.7rem 1.2rem;
  border-radius: 8px;
  border: 1px solid var(--border);
  transition: 0.3s;
}

/* PRIMARY BUTTON */
.btn {
  background: var(--primary);
  color: white;
}

/* WHATSAPP */
.btn-whatsapp {
  display: flex;
  align-items: center;
  gap: 0.5rem;
}

/* HOVER BUTTONS */
.btn:hover,
.btn-whatsapp:hover {
  transform: translateY(-3px);
  border-color: var(--primary);
}
</style>